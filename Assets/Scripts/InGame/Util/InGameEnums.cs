public enum eButtonType
{
    Save, Load, Log, Auto, Hide, Pause
}

public enum eInputType
{
    Play
}

public enum eBackgroundAction
{
    CHANGE, SHAKE
}

public enum eBackgroundEffect
{
    IMMEDIATE, LEFTRIGHT
}