public enum eScenarioGroup
{
    NONE, START, END
}

public enum eScenarioTarget
{
    NONE, CHARACTER, BACKGROUND, IMAGE, SOUND, TEXT
}

public enum eScenarioColumn
{
    NONE, GROUP, NAME, TEXT, TARGET, ID, ACTION, EFFECT, POSITIONX, POSITIONY, DELAY
}